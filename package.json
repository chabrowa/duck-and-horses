{
  "name": "duck-and-horses",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "0.9.0"
  },
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "cd build && git init && touch .nojekyll && git checkout -b gh-pages  && git add . && git commit --allow-empty -m ':rocket: deploy' && git push https://github.com/chabrowa/duck-and-horses gh-pages --force"
  }
}
